/*
 * RoamRIT — Supabase connection settings
 * -------------------------------------------------
 * Fill these two values in with the ones from your own
 * Supabase project: Project Settings → API.
 *
 * SUPABASE_URL   → "Project URL"
 * SUPABASE_ANON_KEY → "anon" / "public" key (NOT the service_role key)
 *
 * The anon key is safe to ship in client-side code — it has no
 * power on its own. Access is controlled entirely by the Row
 * Level Security (RLS) policies you run in schema.sql. Never put
 * the service_role key in this file or anywhere in the frontend.
 */
window.SUPABASE_URL = "https://secbidjxomybpqgivfwe.supabase.co";
window.SUPABASE_ANON_KEY = "sb_publishable__ZCr76qJ4dPAgNYJuBWeew_19HgXX77";
